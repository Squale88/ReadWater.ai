Anchor metadata:
  id: {anchor_id}
  type: {structure_type}
  scale: {scale}
  rationale: {rationale}

Grid: {grid_rows} rows (A..{last_row}) x {grid_cols} columns (1..{grid_cols}).

The anchor's core cells, its local-complex member cells, and the influence zone's cells have already been drawn on the mosaic for your reference. Now pick up to 4 compact fishable subzones.

Valid subzone types (v1): drain_throat, point_tip, oyster_bar_edge, pocket_mouth, island_tip_seam.

```json
{{
  "subzones": [
    {{
      "subzone_id": "{anchor_id}-s1",
      "subzone_type": "drain_throat",
      "cells": ["E4"],
      "relative_priority": 1.0,
      "reasoning_summary": "tightest current funnel at the confluence",
      "confidence": 0.85
    }}
  ]
}}
```
